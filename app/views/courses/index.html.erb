<%= render partial:  '/courses/list', 
            locals:   { courses:    @courses, 
                        as:              'course', 
                        owner:            nil, 
                        show_when_empty:  true, 
                        show_title:       false, 
                        show_new:         true, 
                        show_pages:       true
                       } %>


<% if false %>

<%= render partial:  '/courses/table', 
            locals:   { courses:    @courses, 
                        as:              'course', 
                        owner:            nil, 
                        show_when_empty:  true, 
                        show_title:       true, 
                        show_new:         true, 
                        show_pages:       true
                       } %>

  <%= render partial: '/courses/deck', collection: @courses, as: :course %>
<% end %>