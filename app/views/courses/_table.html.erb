<% if show_when_empty or courses.size > 0 %>
	<% new_course = Course.new %>

	<div class="row rrow section-header">
    <% if show_title %>
	  <div class="span0">
	    <h3><%= as.pluralize.humanize %></h3>
	  </div>
    <% end %>
  
    <% if show_new %>
      <% new_course = Course.new %>
      <div class="span0">
      <% if false %>
        <h3><%= render partial: '/courses/new_modal', 
                        locals:  { course: new_course, 
                                    owner:owner, 
                                    as:as} %></h3>
      <% end %>

      <h3>
      <%= link_to new_course_path(ref:request.path), :class => 'edit_button' do %>
        <span class="fa fa-plus fa-lg"></span>
      <% end %>
      </h3>

      </div>
    <% end %>
 
    
	</div>

	<% if courses.class.name == "ActiveRecord::Relation" and show_pages %>
	  <%= will_paginate courses %>
	<% end %>  

	<div class="table-responsive">
	  <table class="table table-hover table-striped">
	    <%= render partial: '/courses/header' %>

	    <tbody>
      <%= render partial: '/courses/row', collection: courses, as: :course %>
	    </tbody>
	  </table>
	</div>

	<% if false %>
	  <!-- add in a new page, not modal -->
	  <%= link_to new_course_path, :class => 'btn btn-default' do %>
	    <span class="fa fa-plus"></span>
	    Add Course
	  <% end %>
	<% end %>  

<% end %>  
